public class Main {
    public static void main(String[] args) {
        StudySession session = new StudySession();
        session.execute(); 
        session.execute(); 
        session.execute(); 
        session.execute();
    }
}